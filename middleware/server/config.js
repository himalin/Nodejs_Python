"use strict";

module.exports = {
    // tcp listening port
    tcpPort: 8153,
    // websocket listening port
    wsPort: 8155,
    // max size of message frame, in chars(not bytes)
    maxFrameSize: 10000,
};